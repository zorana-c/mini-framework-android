<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 系统属性-->
    <!-- 主题颜色-->
    <color name="colorPrimary">#ff000000</color>
    <!-- 操作栏颜色-->
    <color name="colorPrimaryDark">@color/colorPrimary</color>
    <!-- 窗体背景颜色-->
    <color name="windowBackground">@color/colorPrimary</color>
    <!-- 装饰背景颜色-->
    <color name="decorBackground">@color/colorPrimary</color>
    <!-- 控件选中颜色-->
    <color name="colorAccent">@color/colorPrimary</color>
    <!-- 分割线颜色-->
    <color name="colorLineBar">@android:color/transparent</color>
    <!-- 状态栏颜色-->
    <color name="colorStatusBar">@android:color/transparent</color>
    <!-- 导航栏颜色-->
    <color name="colorNavigationBar">@android:color/transparent</color>

    <!-- ToolsBar 返回键Icon颜色-->
    <color name="colorBackIcon">@android:color/white</color>
    <!-- ToolsBar 返回键Text颜色-->
    <color name="colorBackText">@android:color/white</color>
    <!-- ToolsBar 标题Icon颜色-->
    <color name="colorTitleIcon">@android:color/white</color>
    <!-- ToolsBar 标题Text颜色-->
    <color name="colorTitleText">@android:color/white</color>
    <!-- ToolsBar 菜单Icon颜色-->
    <color name="colorMenuIcon">@android:color/white</color>
    <!-- ToolsBar 菜单Text颜色-->
    <color name="colorMenuText">@android:color/white</color>
</resources>