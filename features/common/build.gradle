apply from: "$rootDir/config/config-library.gradle"

android {
    namespace 'com.common'
}

dependencies {
    api project(':library-common')
    api project(':library-core')
    api project(':library-widget')
}